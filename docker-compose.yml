version: "3.8"

services:
  app:
    build: .
    container_name: goba-invest
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - ./static/cache:/app/static/cache
      - ./app.log:/app/app.log
    restart: unless-stopped
